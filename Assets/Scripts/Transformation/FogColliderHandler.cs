using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// A handler for the fog colliding with other <see cref="GameObject"/>s and
/// handling the triggering of transformations.
/// </summary>
public class FogColliderHandler : MonoBehaviour {
	// Start is called before the first frame update
	void Start() {

	}

	// Update is called once per frame
	void Update() {

	}
}
